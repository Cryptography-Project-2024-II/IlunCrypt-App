package com.iluncrypt.iluncryptapp.models.enums;

/**
 * Enum to define how whitespace characters should be handled in encryption.
 */
public enum WhitespaceHandling {
    PRESERVE,   // Keep spaces and whitespace characters as they are
    REMOVE      // Eliminate all spaces and whitespace characters
}
